# 🚀 Como Iniciar o Projeto Next.js

## ✅ Node.js Instalado - Próximos Passos

### Passo 1: Reiniciar o Terminal (IMPORTANTE!)

Após instalar o Node.js, você **DEVE** reiniciar o terminal:

1. **Feche o terminal atual** no Cursor
2. **Feche completamente o Cursor** (File > Exit ou Alt+F4)
3. **Reabra o Cursor**
4. **Abra um novo terminal** (Terminal > New Terminal)

### Passo 2: Verificar Instalação

No novo terminal, execute:

```powershell
node --version
npm --version
```

Você deve ver números de versão (ex: `v18.17.0` e `9.6.7`)

### Passo 3: Navegar até a Pasta

```powershell
cd nextjs
```

### Passo 4: Instalar Dependências

```powershell
npm install
```

Aguarde a instalação terminar (pode levar 2-5 minutos na primeira vez).

### Passo 5: Iniciar o Projeto

```powershell
npm run dev
```

### Passo 6: Abrir no Navegador

Você verá uma mensagem como:
```
✓ Ready in 2.3s
○ Local:        http://localhost:3000
```

Abra o navegador e acesse: **http://localhost:3000**

---

## 🎯 Alternativa: Script Automático

Se preferir, você pode usar o script automático:

1. **Navegue até a pasta `nextjs`** no explorador de arquivos
2. **Dê duplo clique em `start.bat`**
3. O script fará tudo automaticamente!

---

## ❌ Se o Node.js Ainda Não Funcionar

### Verificar Instalação Manualmente

1. Abra o **Prompt de Comando** (não o PowerShell)
   - Pressione `Win + R`
   - Digite `cmd` e Enter

2. No CMD, digite:
   ```cmd
   node --version
   ```

3. Se funcionar no CMD mas não no PowerShell:
   - **Reinicie o Cursor completamente**
   - Ou use o script `start.bat`

### Reinstalar Node.js

Se não funcionar nem no CMD:

1. Baixe novamente: https://nodejs.org/
2. Escolha a versão **LTS** (Long Term Support)
3. Durante a instalação, certifique-se de marcar:
   - ✅ "Add to PATH"
4. Após instalar, **reinicie o computador** (garantir que o PATH seja atualizado)

---

## 📞 Ajuda

Se ainda tiver problemas, me avise e te ajudo a resolver!

