⚠️ IMPORTANTE: Atualizar Dependências

O package.json foi atualizado para corrigir vulnerabilidades de segurança.

Para aplicar as atualizações, execute:

cd nextjs
npm install

Isso atualizará o Next.js para a versão 14.2.33 que corrige todas as vulnerabilidades.

Após instalar, o servidor continuará funcionando normalmente.

